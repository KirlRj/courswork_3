# Проект "Курсовая работа №3 SkyPro"

## Выполнил Рыльский К. Г.

## Описание:

Проект "Курсовая работа №3 SkyPro" - это выполнение задач на языке Python в рамках курсовой работы курса "Python разработчик"
В данном проекте рассматривается работа с БД
На текущий момент в проекте есть следующая функциональность:
1) Вывод список компаний и количество вакансий
2) Вывод всех вакансий, которые пришли из API
3) Вывод средней зарплаты по всем вакансиям
4) Вывод вакансий, у которых ЗП выше средней
5) Вывод вакансий по ключевому слову
Так же все данные сохраняются в БД

## Установка:

1. Клонируйте репозиторий:
```
git clone https://github.com/KirlRj/courswork_3.git
```
## Использование:
Данную функционально можно использовать в HR

## Тестирование:

Для тестирования использовался фреймворк pytest.
Если у вас его нет, то скопируйте команду для установки.
```
poetry add --group dev pytest
```
Для проверки покрытия тестами, использовалась библиотека pytest-cov.
Если ее нет, то скопируйте команду для установки.
```
poetry add --group dev pytest-cov
```
Для проверки покрытия тестами используйте команду.
```
poetry run pytest --cov
```
Для формирования отчета об покрытии тестами в формате HTML, используйте команду.
```
pytest --cov=src --cov-report=html
```
где src - пакет с модулями, которые тестируются.
Отчет будет находиться в htmlcov/index.html